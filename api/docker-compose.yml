version: "3.9"

services:
  attrition-api:
    image: attrition-api:latest
    container_name: attrition-api
    ports:
      - "8000:8000"
    env_file:
      - ../.env                  # explicitly load .env from project root
    restart: unless-stopped